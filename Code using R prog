Appendices:
Appendix A: Code Snippets


# Load necessary libraries library(ggplot2) library(dplyr)

# Load the dataset
cancer_data <- read.csv("cancer_dataset.csv")


# View the first few rows of the dataset
head(cancer_data)


# 1. Histogram: Distribution of Tumor Size
histogram_tumor_size <- ggplot(cancer_data, aes(x = tumor_size)) + geom_histogram(binwidth = 2, fill = "blue", color = "black") + labs(title = "Distribution of Tumor Size",
x = "Tumor Size (mm)", y = "Frequency") +
theme_minimal()


print(histogram_tumor_size)


# 2. Line Chart: Survival Time Over Years # Aggregate survival time by year survival_by_year <- cancer_data %>% group_by(year) %>%
summarise(mean_survival_time = mean(survival_time, na.rm = TRUE))


line_chart_survival <- ggplot(survival_by_year, aes(x = year, y = mean_survival_time)) +
 
geom_line(color = "red") + geom_point(color = "darkred") +
labs(title = "Average Survival Time Over Years", x = "Year",
y = "Mean Survival Time (Months)") + theme_minimal()

print(line_chart_survival)


# 3. Scatter Plot: Tumor Size vs. Survival Time
scatter_plot <- ggplot(cancer_data, aes(x = tumor_size, y = survival_time)) + geom_point(aes(color = Age), alpha = 0.6) +
labs(title = "Tumor Size vs. Survival Time", x = "Tumor Size (mm)",
y = "Survival Time (Months)", color = "Age") +
theme_minimal()


print(scatter_plot)



Appendix B: User Manual
•	Step 1: Install R and RStudio from their official websites.
•	Step 2: Install necessary R packages
using install.packages("ggplot2") ,”dplyr()”and install.packages("plotly").
•	Step 3: Load the dataset into R using read.csv() or a similar function.
•	Step 4: Use the provided code snippets to generate histograms, line charts, and scatter plots.
•	Step 5: Customize the visualizations by modifying the ggplot2 code (e.g., changing colors, labels, or themes).
